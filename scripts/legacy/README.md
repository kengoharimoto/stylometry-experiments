This directory contains older, one-off, or superseded helper scripts.

They are retained for reference, but they are not the recommended public entry points for reproducing the repository's main workflows.

Use the top-level `README.md` and `Makefile` for the primary supported commands.
